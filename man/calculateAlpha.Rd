% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SDDGP_alpha.R
\name{calculateAlpha}
\alias{calculateAlpha}
\title{Calculate Dirichlet Alpha Prior Parameters}
\usage{
calculateAlpha(
  ref_matrix,
  spatial_count,
  spatial_coords = NULL,
  method = "information_weighted",
  concentration = 2,
  pseudocount = 1e-04,
  min_alpha = 0.001,
  normalize_spatial = TRUE,
  use_spatial_smoothing = TRUE,
  spatial_bandwidth = 0.2
)
}
\arguments{
\item{ref_matrix}{Normalized reference basis matrix (genes x cell types).}

\item{spatial_count}{Spatial gene expression count matrix (genes x spots).}

\item{spatial_coords}{Data frame or matrix of spot spatial coordinates
(used for spatial smoothing). Default \code{NULL}.}

\item{method}{Character; currently only \code{"information_weighted"} is
supported.}

\item{concentration}{Base concentration parameter for the Dirichlet prior.
Default 2.0.}

\item{pseudocount}{Small value added for numerical stability. Default 1e-4.}

\item{min_alpha}{Minimum allowed alpha value per cell type. Default 1e-3.}

\item{normalize_spatial}{Logical; whether to CPM-normalize the spatial data.
Default \code{TRUE}.}

\item{use_spatial_smoothing}{Logical; whether to apply spatial smoothing to
the prior estimates. Default \code{TRUE}.}

\item{spatial_bandwidth}{Bandwidth for the Gaussian smoothing kernel.
Default 0.2.}
}
\value{
Named numeric vector of alpha parameters (one per cell type).
}
\description{
Computes information-weighted Dirichlet prior parameters for each cell type
by combining gene specificity (entropy-based), expression intensity,
spatial-reference similarity (weighted Pearson correlation), and optional
spatial smoothing via k-nearest neighbours.
}
