% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SDDGP_refMatrix.R
\name{buildReferenceMatrix}
\alias{buildReferenceMatrix}
\title{Build Reference Expression Matrix from Single-Cell Data}
\usage{
buildReferenceMatrix(
  sc_count,
  spatial_count,
  cell_type,
  ct_varname = "celltype",
  min_count_gene = 100,
  min_count_spot = 3,
  n_pseudo_per_type = 200,
  pseudo_seed = 42
)
}
\arguments{
\item{sc_count}{Gene-by-cell count matrix (dense or sparse).}

\item{spatial_count}{Gene-by-spot count matrix (dense or sparse).}

\item{cell_type}{A data frame containing cell type annotations.}

\item{ct_varname}{Character string specifying the column name in
\code{cell_type} that holds cell type labels. Default \code{"celltype"}.}

\item{min_count_gene}{Minimum number of cells a gene must be detected in
to pass quality control. Default 100.}

\item{min_count_spot}{Minimum number of spots a gene must be detected in.
Default 3.}

\item{n_pseudo_per_type}{Number of pseudo-cells to sample per cell type
for Stage 2. Default 200.}

\item{pseudo_seed}{Random seed for pseudo-cell sampling reproducibility.
Default 42.}
}
\value{
A list with components:
  \describe{
    \item{basis}{Normalized reference expression matrix
      (informative genes x cell types).}
    \item{selected_genes}{Character vector of selected informative genes.}
    \item{cell_types}{Character vector of retained cell types.}
    \item{median_lib_size}{Median library size of the spatial data
      (informative genes only).}
    \item{sc_sampled}{Count matrix of sampled pseudo-cells
      (all filtered genes x pseudo-cells).}
    \item{pseudo_type_labels}{Cell type label for each pseudo-cell.}
  }
}
\description{
Constructs a cell type-specific reference expression matrix from
single-cell RNA-seq data, performs quality control, selects informative
marker genes, and samples pseudo-cells for Stage 2 refinement.
}
